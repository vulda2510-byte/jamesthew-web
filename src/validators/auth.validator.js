// src/validators/auth.validator.js
const validateLogin = (req, res, next) => {
    next();
};

const validateRegister = (req, res, next) => {
    next();
};

module.exports = {
    validateLogin,
    validateRegister
};